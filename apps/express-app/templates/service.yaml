apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-app.appName" . }}{{ .Values.service.nameSuffix }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "express-app.labels" . | nindent 4 }}
    app.kubernetes.io/name: {{ include "express-app.appName" . }}{{ .Values.service.nameSuffix }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "express-app.selectorLabels" . | nindent 4 }}